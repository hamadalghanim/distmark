---
- name: Deploy Docker containers to GCP instances
  hosts: all
  become: yes
  vars:
    ansible_python_interpreter: /usr/bin/python3

  tasks:
    - name: Update apt cache (Debian/Ubuntu)
      apt:
        update_cache: yes
        cache_valid_time: 3600  # skip if cache is less than 1 hour old
      when: ansible_facts['os_family'] == "Debian"

    - name: Check if Docker is installed
      command: docker --version
      register: docker_check
      failed_when: false
      changed_when: false

    - name: Install Docker dependencies
      apt:
        name:
          - ca-certificates
          - curl
          - gnupg
          - python3-pip
          - python3-venv
          - python3-docker
          - rsync
        state: present
      when: docker_check.rc != 0

    - name: Create directory for apt keyrings
      file:
        path: /etc/apt/keyrings
        state: directory
        mode: "0755"
      when: docker_check.rc != 0

    - name: Download Docker's official GPG key
      get_url:
        url: "https://download.docker.com/linux/{{ ansible_facts['distribution'] | lower }}/gpg"
        dest: /etc/apt/keyrings/docker.asc
        mode: "0644"

    - name: Add Docker repository to Apt sources
      copy:
        dest: /etc/apt/sources.list.d/docker.sources
        content: |
          Types: deb
          URIs: https://download.docker.com/linux/{{ ansible_facts['distribution'] | lower }}
          Suites: {{ ansible_facts['distribution_release'] }}
          Components: stable
          Signed-By: /etc/apt/keyrings/docker.asc
        mode: "0644"
      register: docker_repo
      when: docker_check.rc != 0

    - name: Update apt cache (Debian/Ubuntu)
      apt:
        update_cache: yes
      when: ansible_facts['os_family'] == "Debian" and docker_check.rc != 0

    - name: Install Docker dependencies
      apt:
        name:
          - ca-certificates
          - curl
          - gnupg
          - python3-pip
          - python3-venv
          - python3-docker
          - rsync
        state: present
      when: docker_check.rc != 0

    - name: Install Docker Engine
      apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-buildx-plugin
          - docker-compose-plugin
        state: present
      when: docker_check.rc != 0
    - name: Start Docker service
      service:
        name: docker
        state: started
        enabled: yes

    - name: Add user to docker group
      user:
        name: "{{ ansible_user }}"
        groups: docker
        append: yes

    - name: Copy service directory
      synchronize:
        src: "../{{ service }}/"
        dest: "/home/{{ ansible_user }}/{{ service }}/"
        delete: yes
        use_ssh_args: yes # tells rsync to use ansible's ssh args including ProxyCommand

    - name: Build Docker image
      docker_image:
        name: "{{ service }}"
        source: build
        build:
          path: "/home/{{ ansible_user }}/{{ service }}"
        state: present
        # Force a rebuild if code changed
        force_source: yes

    - name: Stop existing container
      docker_container:
        name: "{{ service }}"
        state: absent
      ignore_errors: yes

    - name: Run Docker container
      docker_container:
        name: "{{ service }}"
        image: "{{ service }}"
        state: started
        restart_policy: always
        ports: "{{ service_ports | default([]) }}"
        env: "{{ container_env | default({}) }}"
